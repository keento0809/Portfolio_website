import Backdrop from "./Backdrop";
import { MockProvider } from "../Button/Button.stories";

const Meta = {
  component: Backdrop,
  title: "Backdrop",
  tags: ["autodocs"],
  //   decorators: [MockProvider],
};

export default Meta;

export const Default = {
  args: {},
};
